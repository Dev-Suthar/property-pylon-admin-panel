# API Configuration
# Backend API base URL
# Default: http://98.92.75.163:3000/api/v1
# Override this in production if your API is hosted elsewhere
VITE_API_URL=http://98.92.75.163:3000/api/v1

# Example for production with HTTPS:
# VITE_API_URL=https://api.dreamtobuy.com/api/v1

# Note: All environment variables in Vite must be prefixed with VITE_
# These variables are embedded at build time, not runtime
